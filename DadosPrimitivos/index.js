const nome = "Erick"; // string
const nome1 = "Erick"; // string
const nome2 = `Erick`; //string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined -> não aponta para local nenhum na memoria
const sobremoneAluno = null; // Nulo -> não aponta para local nenhum na memoria
const aprovado = false; // Boolean = true, false (lógico)
